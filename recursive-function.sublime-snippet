<snippet>
	<description>Recursive function</description>
    <content><![CDATA[
func rec(str string, data []int, k int) int {
     if terminal condition {
        return 0
     }

     for _, next := range NEIGHBOURS {
        if next is interesting {
             return 1 + rec( str, next, k+1 )
        }
     }
}
]]></content>
</snippet>
